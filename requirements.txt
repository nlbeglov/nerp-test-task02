# Flask - сам веб-фреймворк, на нём написан src/app.py
Flask==3.0.3

# gunicorn - production-сервер для запуска Flask внутри Docker-контейнера.
# Встроенный сервер Flask (app.run(...)) не предназначен для реальной
# нагрузки - он однопоточный и не показывает нормальные логи в проде.
gunicorn==22.0.0

# pytest - фреймворк для тестов (tests/test_app.py)
pytest==8.2.2
